package com.RP.ControleDeJornada.domain.repository;

import com.RP.ControleDeJornada.domain.entitys.team.Rc;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RcRepository extends JpaRepository<Rc, StringBuilder> {
}
